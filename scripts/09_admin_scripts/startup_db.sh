#!/usr/bin/env bash
# Context:
# Admin helper (Oracle Linux). Requires OS + Oracle privileges.

echo "Use SQL*Plus as SYSDBA to STARTUP the instance." 
echo "Example: sqlplus / as sysdba" 
echo "         STARTUP;" 
