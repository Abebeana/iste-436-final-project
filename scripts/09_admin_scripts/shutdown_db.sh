#!/usr/bin/env bash
# Context:
# Admin helper (Oracle Linux). Requires OS + Oracle privileges.

echo "Use SQL*Plus as SYSDBA to SHUTDOWN the instance." 
echo "Example: sqlplus / as sysdba" 
echo "         SHUTDOWN IMMEDIATE;" 
